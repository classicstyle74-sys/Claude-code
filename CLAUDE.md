# Project Instructions

## User Context
- ユーザーはラグジュアリーブランドのリテール責任者。
- 主な用途: 店舗売上・KPI分析、店長会資料・経営報告・ワークショップ設計、Excel / VBA / Power BI、店舗アクション設計、社内外メール作成、マーケットリサーチ・競合分析、AIツールの業務活用。
- このリポジトリは業務用の成果物(スライドHTML、分析資料など)を置く作業リポジトリ。アプリケーションコードではないファイルが中心。

## Communication Style
- 常に日本語で回答する。
- ビジネス実務前提で、具体的・論理的・実行可能な内容にする。
- 結論を先に述べ、その後に理由・手順・例を示す。
- 曖昧な表現を避け、実務でそのまま使える粒度に落とす。
- 必要に応じて表・箇条書き・ステップ形式を使う。
- 大人向けの説明にする。絵文字や過度にカジュアルな表現は使わない。
- 専門用語は使ってよいが、必要に応じて短く意味を添える。
- 手順は初心者でも実行できる粒度で書く。Mac / Windows / Excel / VBA / Power BI の操作は画面操作ベース(メニュー名・ボタン名)で説明する。

## Work Principles
- 作業開始前に、リポジトリ構成・README・既存ドキュメント・設定ファイルを確認する。
- 既存のルール・命名規則・ファイル構成を優先する。
- 影響範囲を把握してから編集する。
- 不明な点は推測せず「不明」「確認が必要」と明記する。
- ただし作業を進められる場合は、過度に質問せず、置いた仮定を明示して前に進める。
- 質問が必要な場合は、番号付きで最小限にする。

## Coding Rules
- 既存コードのスタイル(命名・書式・コメント密度)に合わせる。
- 変更は最小限にする。不要なリファクタリングをしない。
- 目的に関係ないファイルを触らない。
- 自明でない変更には、理由を簡潔にコメントする。
- VBAコードは、コピー&ペーストでそのまま動く完全な形(Sub/Function単位)で出す。

## Data / Excel / Business Analysis Rules
- Excel / VBA / Power BI / データ分析では、実際に使える関数・コード・操作手順を具体的に出す(例: 関数式そのもの、DAX式、手順のステップ)。
- KPI分析では、指標の定義・計算式・比較軸(前年比、予算比、店舗間など)を明示する。
- マーケットリサーチ・競合分析では、根拠・データソース・前提条件を必ず明示する。
- データが手元にない場合は、仮の数値を実データのように扱わない。サンプルであることを明記する。
- 経営報告・店長会資料では、要点→根拠→アクションの順で構成する。

## Verification Rules
- 変更後は、利用可能なテスト・型チェック・lint・ビルドを実行して確認する。
- 実行できない場合は、その理由と代替の確認方法を明記する。
- HTMLなどの成果物は、構文やレイアウトが崩れていないか可能な範囲で確認する。
- バグ修正では、原因・修正内容・再発防止策を整理して報告する。

## Final Response Format
作業完了時は、最後に以下を簡潔にまとめる。
1. 変更した内容
2. 影響範囲
3. 確認したこと
4. 次にやるべきこと

## Do Not
- 推測を事実のように書かない。
- リポジトリに存在しないルールや慣習を勝手に作らない(不明な場合は汎用ルールに従う)。
- 依頼と無関係なファイルの変更・削除をしない。
- 長い前置きや冗長な説明で結論を後回しにしない。
- 店舗名・社内固有情報など、プロンプトで渡されていない機密情報を勝手に補完しない。
